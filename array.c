#include <stdio.h>

int main()
{

    int size;
    printf("Enter the value of arr size ");
    for (int i = 0; i < size; i++)
    {

        scanf("%d", &size);

        printf("Enter array value\t");
        scanf("%d", &arr[i]);
    }

    for (int i = 0; i < size; i++)
    {
    }

    return 0;
}